# Frontend React - داشبورد آزمایشگاه

این Frontend برای نمایش داده‌های تمیز از Backend Middleware استفاده می‌کند.

## 🚀 نصب و راه‌اندازی

### 1. نصب وابستگی‌ها:
```bash
cd frontend
npm install
```

### 2. تنظیم متغیرهای محیطی (اختیاری):
اگر Backend روی پورت یا آدرس دیگری اجرا می‌شود، فایل `.env` بسازید:
```env
VITE_API_URL=http://localhost:3001
```

### 3. اجرای Frontend:
```bash
npm run dev
```

Frontend روی `http://localhost:3000` اجرا می‌شود.

### 4. Build برای Production:
```bash
npm run build
```

فایل‌های build در پوشه `dist` قرار می‌گیرند.

## 📁 ساختار پروژه

```
frontend/
├── src/
│   ├── components/
│   │   ├── Login.jsx          # فرم ورود
│   │   ├── Dashboard.jsx      # داشبورد اصلی
│   │   ├── TestList.jsx       # لیست آزمایش‌ها
│   │   ├── TestCharts.jsx     # نمودارها
│   │   └── BioneurologyAnalysis.jsx  # تحلیل بیونورولوژی
│   ├── App.jsx                # کامپوننت اصلی
│   ├── main.jsx               # Entry point
│   └── index.css              # استایل‌های کلی
├── index.html
├── vite.config.js
└── package.json
```

## 🎨 ویژگی‌ها

### 1. فرم ورود
- ورود با کد ملی و رمز عبور
- مدیریت خطا و نمایش پیام‌های مناسب
- ذخیره توکن در localStorage

### 2. داشبورد
- سه تب: لیست، نمودارها، تحلیل
- نمایش لیست کامل آزمایش‌ها
- نمودارهای تعاملی با Recharts
- تحلیل هوشمند بیونورولوژی

### 3. نمودارها
- نمودار میله‌ای وضعیت آزمایش‌ها
- نمودار خطی روند آزمایش‌ها در طول زمان
- نمایش چندین آزمایش به صورت جداگانه

### 4. تحلیل بیونورولوژی
- آمار کلی (نرمال، غیرنرمال، بالا، پایین)
- تحلیل روند (بهبود، کاهش، پایدار)
- دسته‌بندی آزمایش‌ها
- توصیه‌های هوشمند

## 🔧 تکنولوژی‌ها

- **React 18**: کتابخانه UI
- **React Router**: مدیریت مسیرها
- **Axios**: درخواست‌های HTTP
- **Recharts**: نمودارها
- **Vite**: Build tool و Dev server

## 📱 Responsive Design

Frontend به صورت responsive طراحی شده و روی موبایل و تبلت هم به خوبی کار می‌کند.

